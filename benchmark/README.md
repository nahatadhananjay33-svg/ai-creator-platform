# Cross-engine benchmarks

Entry points that combine multiple engines (e.g. end-to-end reel latency) land here from Phase A5. Engine-specific benchmarks live inside each engine (see voice_engine/benchmark/) and this directory only composes them.
